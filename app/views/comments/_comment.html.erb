
    <br>
      <%= comment.content %><br>
      <%= link_to "#{comment.user.name}", comment.user, method: :get%>
      
        <br>Posted <%= time_ago_in_words(comment.created_at) %> ago.

      <% if current_user?(comment.user) %>
        <%= link_to "delete", comment, method: :delete,
                                         data: { confirm: "You sure?" },
                                         content: comment.content %>
      <% end %>

